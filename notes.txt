* Rust is a statically and strongly typed language.
* important commands: 
    cargo build
    cargo run
    cargo check
    rustfmt <filename>
* By default any variable defined in rust are immutable. To make them mutable we need to add "mut" keyword. 
    e.g: let mut x = 4;
